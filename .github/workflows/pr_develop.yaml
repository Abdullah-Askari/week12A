name: wf_pr_develop
on:
  pull_request:
    branches:
      - develop
jobs:
  validate_pr_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome step
      - run: echo "Welcome to the PR validation workflow!"